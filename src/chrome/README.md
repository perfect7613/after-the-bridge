# Chrome

The page the Player looks at: video surface, choice cards, dialogue, ledger panel, toast, copy-prompt, elicitation countdown.

Chrome reads Chapter snapshots and sends Chapter inputs. It does not contain story rules. Elicitation UI resolves the Chapter's pending question; Wren's `ask_player` waits on that resolution.
